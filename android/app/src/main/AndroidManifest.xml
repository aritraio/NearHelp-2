<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Full-screen SOS alert when the device is locked (DESIGN.md §5). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Battery-kill mitigation for Indian OEMs (improvements.md §1.4). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Responder location streaming while en route (Phase 4). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:name=".NearHelpApp"
        android:allowBackup="false"
        android:label="NearHelp"
        android:theme="@style/Theme.NearHelp">

        <!-- Replace with a real Maps key for live map tiles (README). Without
             it the app still works — the incident screen falls back to the
             distance/ETA panel instead of the map. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="MISSING_MAPS_KEY" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- FCM notifications deep-link into the responder alert screen. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nearhelp" />
            </intent-filter>
        </activity>

        <!-- SOS data messages are rendered app-side (full-screen alert, DRILL
             banners). Inert until Firebase is configured (README); the
             PushRouter also surfaces alerts while the app is foregrounded. -->
        <service
            android:name=".push.NearHelpMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Streams the accepted responder's position over the SOS channel
             with adaptive intervals (todos.md §4). -->
        <service
            android:name=".service.ResponderLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

</manifest>
